// tic-tac-bid!
// by amory acosta and nick marigo

let config = {
	type: Phaser.WEBGL,
	width: 800,
	height: 600,
	backgroundColor: '#FACADE',
	scene: [ Menu ]
};

const game = new Phaser.Game(config);
